resources:
  cloud: azure
  accelerators: V100:1  # 1 V100 GPU, SkyPilot will handle the provider-specific request

setup: |
  pip install -r requirements.txt  # Install dependencies

run: |
  python train.py  # Run the training script

